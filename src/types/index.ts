export enum Gender {
  MALE = "nam",
  FEMALE = "nữ",
}

export enum Majors {
  SECURITY = "An toàn thông tin",
  IT = "Công nghệ thông tin",
  Electronic = "Điện tử viễn thông",
}

export enum AccountType {
  ADMIN = "admin",
  TEACHER = "teacher",
  USER = "user",
}

export enum StatusLetter {
  PENDING = "pending",
  REJECT = "reject",
  SUCCESS = "success",
}

export enum TuitionType {
  NONE = "Không miễn giảm",
  REDUCE = "Giảm học phí",
  FREE = "Miễn học phí",
}

export enum TuitionObj {
  ORPHAN = "Mồ côi",
  NONE_ORPHAN = "Không mồ côi",
}

export enum TimeYear {
  HALF = 6,
  Full = 12,
}

export enum PaymentReason {
  Graduation = "Tốt nghiệp",
  LeaveSchool = "Thôi học",
}
